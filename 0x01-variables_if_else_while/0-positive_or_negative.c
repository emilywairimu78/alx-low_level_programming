#include <stdio.h>
#include <stdlib.h>
#include <time.h>


/**
 * print whether the number stored in the variable n is positive or negative.
 * return is 0
 */
int main (void)
{
	int n;

		srand(time(0));
			n = rand() - RAND_MAX / 2;
				/* your code goes there */
				return (0);
}
